import 'package:flutter/material.dart';

class TermProfilePage extends StatelessWidget {
  const TermProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

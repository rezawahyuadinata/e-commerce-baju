import 'package:flutter/material.dart';

class OrderProfilePage extends StatelessWidget {
  const OrderProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
